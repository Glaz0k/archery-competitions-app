import type { ComboboxItem } from "@mantine/core";

export const DEFAULT_VALUE = "default_select_value";
export const DEFAULT_ITEM: ComboboxItem = {
  value: DEFAULT_VALUE,
  label: "По умолчанию",
};
